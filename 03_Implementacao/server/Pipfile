[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Pinned to actively-supported, patched minimum versions to close the
# Dependabot alerts on this historical-reference-only Django backend
# (V2 replaces it with Firebase; this server is not run or developed further).
django = ">=5.2,<5.3"
psycopg2 = ">=2.9.10"
djangorestframework = ">=3.15.2"

[dev-packages]

[requires]
python_version = "3.10"
