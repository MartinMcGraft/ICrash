rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Locked until Phase 2 introduces and tests institution-scoped rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
